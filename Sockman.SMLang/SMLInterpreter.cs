namespace Sockman.SMLang
{
    public class SMLInterpreter
    {
        public byte[] Interpret(string code)
        {
            List<byte> result = new List<byte>();
            return result.ToArray();
        }
    }
}